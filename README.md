A Figma-style live cursor presence tracker built with Node.js, TypeScript, and the WebSocket protocol. Every connected client broadcasts their mouse position in real time; all other clients render it as a colored dot on an HTML canvas. Mainly to learn concepts of web socket servers.
